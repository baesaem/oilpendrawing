# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 명령어

```bash
npm install
npm run dev        # 개발 서버. base=/ 로 5173 고정, 브라우저 자동 실행
npm run build      # tsc -b (타입 검사) 후 vite build → dist/
npm run typecheck  # 타입 검사만
npm run preview    # 빌드 결과를 4173 에서 서비스
```

테스트 러너는 없다. 변경을 확인할 때는 `npm run build` 로 타입 검사를 통과시키고,
브라우저에서 실제 동작을 본다. UI 회귀는 Playwright 로 임시 스크립트를 써서 확인해 왔다
(제공사 API 는 `page.route` 로 가짜 응답을 물려 놓고 흐름만 검증).

빌드 시 `base` 가 세 갈래다. 개발은 `/`, 기본 빌드는 `/oilpendrawing/`(GitHub Pages 하위 경로),
`VITE_BASE=/` 를 주면 루트(커스텀 도메인용). `VITE_PREVIEW=1` 로 빌드하면 미리보기 모드가 켜진다.

## 앱의 목적

**드로잉 초보자가 사진을 보고 스케치북에 오일펜으로 직접 그릴 때 옆에 두는 참고서**다.
AI 결과물은 완성작이 아니라 "따라 그릴 목표"이고, 앱의 핵심 가치는 그 목표에 이르는
단계를 보여 주는 데 있다. 기능을 더할 때 이 관점을 기준으로 판단한다.

UI 문구는 모두 한국어이고, 코드 주석도 한국어로 쓴다.

## 구조

### 화면 하나

`App.tsx` 가 모든 상태를 들고 있고 화면은 **원본/결과 비교 하나**다. `view` 가 `'compare' | 'result' | 'original'`.
비교 화면의 분할선은 `.compare-grab` 덮개가 포인터를 직접 받아 **그림 위 어디를 잡아도** 끌린다 — 옛 `<input type="range">` 는
브라우저가 손잡이를 요소 위쪽에만 그려서 가운데 원을 잡아도 안 움직였다(사용자 제보). 가운데 원은 지금 위치를 보여 주는 표시일 뿐이다.
좌우 화살표(Shift 로 크게)로도 움직인다.
화면 위 한 줄(`.topbar`)이 [보기 전환(`ViewSeg`)] · 제목 · [그리기 버튼(`DrawActions`)] 이다 — 둘 다 `Toolbar.tsx` 에서
내보내고 아래 툴바에는 없다. 가장 자주 누르는 버튼을 눈에 먼저 들어오는 자리에 둔 것.
격자(`GridSize`·`Overlay`, `components/GridOverlay.tsx`)는 툴바 선택기로 켜고 사진·결과 위에 겹쳐 그린다 —
칸을 보고 스케치북의 같은 칸에 옮겨 그리라는 것이다. `FullscreenView` 도 같은 그림과 격자를 전체화면으로 다시 그린다(G 키로 칸 수 전환).

**옛 "그리기 가이드"(구도 → 큰 형태 → 명암 → 완성 참고 4단계)는 없앴다** — `GuideView`·`guide.ts`·`tips.ts`·`useGuideImage`
파일과 "과정 그림 만들기"(`buildProcessPrompt`)도 함께 지웠다. 격자만 드로잉 화면으로 옮겨 남겼다.
`Drawing.process` 필드는 옛 이력 레코드를 읽기 위해서만 남아 있다.

### API 비용이 드는 곳과 안 드는 곳

기본 경로는 전부 브라우저 계산이다. "그리기 시작"은 `render.ts` 의 `renderDrawing`, 견본 분석은 `sampleStyle.ts` 의 `analyzeSample`.
뒤의 둘은 순수 함수(DOM 없음)라 `render.worker.ts` 에서 돌고, `local.ts` 가 Blob ↔ ImageData 변환과
워커 호출을 맡는다. 워커 스크립트를 못 불러오거나 워커가 죽으면(`onerror`) 콘솔에 위치를 찍고 `workerBroken` 을 세워
그 뒤로는 화면 스레드에서 같은 함수를 직접 돈다 — 그래서 "그리기 시작"은 어떤 환경에서도 결과를 낸다. API 를 쓰는 건 "AI로 그리기" 하나뿐이고 키가 있어야 켜진다.
이 경계를 흐리지 않는다.

### 로컬 엔진 (Dynamic Auto-Painter 방식)

`render.ts` 하나가 엔진이고 `PaintProfile`(`types.ts`)이 엔진이 보는 값의 전부다: 붓(`brush`), 층 수, 획 크기, 세밀함, 정밀도,
획 길이, 형태 따라가기, 기준 각도, 무작위성, 선 굵기, 잉크 농도, 여백, 윤곽선, 가장자리 여백, 종이·잉크색.
DAP(Hertzmann 의 "여러 크기의 굽은 획" 페인팅)를 펜에 맞게 구현한 것이라 흐름이 DAP 와 같다:

1. **목표** — 밝기에서 어둡기 지도 `target` 을 만든다. `paperKeep` 위는 종이(0), 나머지는 감마를 두어 사진보다 옅게, 잔결 영역은 더 옅게.
2. **층(pass)** — `passSizes` 가 획 크기를 `brushSize`(첫 층)에서 `detail`(마지막 층)까지 등비로 줄인다. 층마다 목표를 획 크기의 절반만큼
   뭉갠 참조 `ref` 를 보고(큰 획은 큰 형태만), `sweep` 이 캔버스를 격자(`max(선굵기×2.2, R×0.3)`)로 나눠 **아직 목표보다 밝은 칸**만
   골라(칸 평균 오차 > `T`, `T` 는 정밀도에서) 가장 차이가 큰 자리에 획을 놓는다. 칸 순서는 무작위.
**붓은 여덟 가지다**: `tone`(펜화 — 명암 단계 해칭), `pen`(리천), `contour`(윤곽), `stipple`(점묘), `inkwash`(수묵담채), `wash`(담채), `oil`(유화),
`impasto`(고흐). 옛 `hatch`·`cross`·`scribble` 은 지웠다 — 평행선·교차선은 `tone` 이 더 잘 하고 스크리블은 `pen` 의 무작위성으로 대신한다.
옛 레코드는 `migrateStrokes`(`fill` → 붓)와 `mergeParams`(`OLD_BRUSH`)가 옮긴다.

**점묘 붓**(`brush: 'stipple'`)의 점 하나는 **선 굵기 슬라이더가 곧 점 굵기**다(지름 ≈ `lineWidth × 1.24`, 패널에도 "점 굵기"로 나온다).
가장자리는 `Canvas.dotRough` 가 각도별로 반지름을 흔들고 테두리 화소를 무작위로 빠뜨려 점마다 다르게 거칠다(거칠기 0.16~0.66, 점마다 무작위) —
매끈한 원이 늘어서면 인쇄 망점처럼 보인다. 컬러에서는 **그 자리 사진 색 그대로**(뭉개지 않은 원본 화소, 팔레트만 거친다) 찍는다:
잉크색을 섞지 않고(`ki=0`) 거의 불투명하게 얹으며, 점 사이로 종이가 비쳐 그림이 바래지 않도록 목표 어둡기를 `target^0.55` 로 올리고
무작위로 흩뿌려 겹치는 몫만큼 점 수를 1.6배로 잡는다. 흑백은 잉크 농도만 ±14% 흔든다.

**펜화 붓**(`brush: 'tone'`, 화풍 `tonehatch`)만은 위 층·격자 구조를 쓰지 않고 `toneHatch` 로 따로 그린다 — 드로잉 교본의 값 스케일 그대로다:
밝기를 뭉갠 뒤 `passes` 단계(기본 15, 3~20)로 나누고, 단계 k 마다 **그 단계 이상 어두운 곳 전체**에 한 방향 평행선을 긋는다. 층이 겹치므로 어두울수록
선이 여러 벌 쌓이고(선 갯수), 층마다 간격을 좁히고(가장 어두운 층은 첫 층의 1/3 — `S0·(1 − 0.66·t)`, 선 굵기의 1.1배 아래로는 안 좁힌다)
굵기를 키우며(`lw·(0.55 + 1.6·t)` — 밝은 단계는 가는 선, 어두운 단계는 굵은 선. "가는 선과 굵은 선"이 한 그림에 함께 보이도록 폭을 넓게 잡았다), 3층부터 각도를 `ANGLE_OFF`(+52°·−41°·+88°·+24°·−68°·+14°·+70°·−25°·+105°·+38°·−52°·+80°·−13°·+96°·+30°·−78°·+62°·+8°, 20단계까지) 로 틀어 교차선이 된다.
가장 밝은 단계는 선이 없다 — 흰 종이. 선은 화면을 가로지르는 긴 직선이고 마스크 안에서만 자국을 남기며, 완만한 흔들림·필압 변화·가끔의 펜 떼기로
손그림처럼 보이게 한다. 사용자가 사진 보고 스케치북에 그대로 옮길 수 있는 유일한 방식이라 갤러리 첫 칸에 둔다.
`baseAngle` 이 전체 방향, `detail` 이 선 간격, `passes` 가 단계 수다 — 이 중 패널에 나오는 것은 선 간격과 명암 단계뿐이고,
`baseAngle` 슬라이더는 두지 않는다(각도는 화풍 프리셋이 정하고 바꿀 때는 툴바의 "방향 지시"로 직접 긋는다).
**방향 지시선**은 선을 휘게 하지 않는다 — 휘게 하면 선이 한쪽으로 몰려 구멍이 생긴다(시험함). 대신 지시선이 센 자리(`man` ≥ 0.3)를
각도 12칸(15°)으로 나눠 표시하고, 칸마다 **그 각도의 곧은 평행선을 따로** 긋는다. 지시선이 없는 곳은 기준 각도로 긋는다 —
사람이 벽은 세로, 바닥은 원근 방향으로 나눠 긋는 것과 같다. `drawSet(각도, 간격, 굵기, 위상, keep)` 하나로 두 경우를 다 그린다.

3. **획** — `penStroke` 는 씨앗에서 양쪽으로 뻗으며 `dirAt`(형태 따라가기 × 방향장 확실성으로 기준 각도와 두 배 각 혼합)을 조금씩 따라
   굽고, 목표가 `tol` 만큼 밝아지는 곳(다른 면)이나 캔버스가 이미 충분히 어두운 곳에서 멈춘다 — Hertzmann 의 정지 조건.
   붓별 훑기 구성은 `sweepsFor`: pen 은 [기본, 그림자에만 90°], hatch 는 [기본, 반 칸 어긋난 기본], cross 는 [0°, 90°, 그림자에 45°],
   contour 는 깊은 그림자만 성글게, scribble 은 `scribbleStroke`(곡률 무작위 고리), stipple 은 부족한 만큼 점을 흩뿌림.
   pen 붓은 잔결(`texture` > 0.6)에서 획 대신 `loopStroke`(나뭇잎 고리선). 잉크 농도 85 이상이면 가장 깊은 그림자를 먹으로 채운다.
4. **윤곽** — `edgePass` 가 경계가 센 자리에서 출발해 접선을 따라 양쪽으로 긋고, 지나간 자리는 획 번호로 표시해 같은 경계를
   두 번 긋지 않는다 (자기 자국에 막히지 않도록 번호를 비교한다). 경계 세기는 둘의 최대다: 색 경계(세 채널 Sobel 제곱합, 잔결에서는
   `1+2.2·texture` 로 나눠 센 것만)와 색상 경계(`chromaEdgeMag`, 밝기를 뺀 색도의 그라디언트 — 잎끼리는 작고 지붕·기둥·노란 나무 vs
   녹색은 커서 잔결 속의 구조물을 살린다). 뭉갠 밝기의 경계를 항으로 넣어 봤으나 실사에서는 잎 덩어리마다 굵은 벌레 같은 윤곽이 생겨 뺐다.
   접선은 잔결 영역에서는 지역 방향장이 소음이므로 뭉갠 밝기의 그라디언트에 수직인 방향을 쓴다.
5. **담채**(`brush: 'wash'`) 는 DAP 그대로다: `washTarget`(여백 문턱 아래의 사진 색을 조금 뭉개고 채도를 1.6배 살려 종이에 곱한 목표 색.
   펜의 어둡기 지도를 쓰지 않는다 — 그러면 물빠진 회색이 된다)과 캔버스의 RGB 차이가
   큰 칸에만 붓 크기 R 의 붓 자국(`Canvas.dab`)을 방향장을 따라 얹고, 다른 면으로 넘어가면 멈춘다. 앞 층들은 젖은 붓((1−d²)² 로 번짐)으로
   큰 색면을, 마지막 두 층은 마른 종이 위(가장자리 또렷)에 얹는다 — 참고 수채화(사용자가 올린 한국 수채화가의 그림: 채도 높고 명암 대비 강하고
   면이 또렷하며 흰 종이가 많이 남는 wet-on-dry)에 맞춘 구성이다. 층이 끝나면 `pigmentEdges` 가 경계를 어둡게(안료 고임) 하고 칠한 곳에
   입자감 얼룩을 넣는다. 그 위의 어두운 붓(`ink` ≥ 30 일 때 깊은 그림자)과 윤곽(임계값 1.5배)은 컬러 모드에서 검정이 아니라 그 자리 색을
   짙게 누른 색이다. DAP 의 Watercolor Wet on Wet(사용자 영상)보다 또렷한 쪽으로 맞췄다.
   **층 순서(DAP 영상 검증)**: 사용자가 올린 DAP Watercolor Wet on Wet 작업 영상을 2초 간격으로 잘라 읽은 순서 — 밑칠(큰 젖은 붓, 지름이
   화면 폭의 9~13%, 옅게 두 번 겹침) → 중간 붓 → Reactor(매끄럽게·음영) → Dry Reveal(마른 작은 붓, 폭의 1.5~3%, 세부 드러냄) → 가장 작은 붓과
   어두운 점 → 후처리(종이·가장자리) — 를 `passes` 를 셋으로 나눠 그대로 따른다: 앞 1/3 은 `wet` 팁 alpha 0.7~0.8 짧은 자국 두 번,
   가운데 1/3 은 중간 붓 뒤 `smoothCanvas`(r1, 65% 섞음), 뒤 1/3 은 마른 팁(젖은 팁이면 `chalk`) alpha 1.1. 붓 크기 범위는 `passSizes`×1.3 이
   같은 사진에서 DAP 와 같은 비율(첫 층 지름 ≈ 폭 14%, 마지막 ≈ 1.5%)임을 확인했다. 진행 표시의 `label` 이 이 단계 이름을 보여 준다.
5-1. **수묵담채**(`brush: 'inkwash'`, 화풍 `inkwash`) 는 담채와 같은 경로를 마른 채로 돈다 — 사용자가 올린 오일펜 수묵담채(숲길·개나리)를 보고 넣었다.
   담채와 다른 곳은 넷이다: (1) 색을 정말 어두운 곳에만 얹는다(문턱 `white × 0.72`, 경계 폭 0.16 → 0.42) — 중간 밝기에서 종이가 그대로 비쳐야
   동양화의 여백이 나온다. (2) 층마다 붓칠을 한 벌만 또렷하게 얹고 밑칠 겹치기·`smoothCanvas`·`waterFlow` 를 건너뛴다(번지지 않는다).
   (3) 세부 마무리 층과 원본 겹치기를 하지 않는다 — 둘 다 흰 종이를 메운다. (4) 그림의 뼈대는 먹선이라 윤곽 문턱을 담채의 1.5배에서 1배로
   낮추고 굵기를 1.4배로 올리며, 짙은 곳(`minRef` 0.66)에만 짧고 굵은 먹획을 얹는다. 긴 획을 그으면 결이 되어 여백이 사라진다(시험함).

6. **유화**(`brush: 'oil'`, 화풍 `oil`) 는 담채와 같은 경로(`washTarget`·`washSweep`)를 불투명 설정으로 돈다: 종이 문턱 없이 전 화면이 목표,
   채도 1.3·대비 1.18, 붓 자국은 얇고(R×0.32) 짧고 또렷하며 alpha 0.8~0.95, 붓 자국마다 밝기가 ±8% 흔들려 붓결이 보인다. 안료 고임·펜·윤곽 없음.
   DAP 의 인상주의 유화 프리셋 결과(사용자가 올린 그림)를 보고 넣었다. 펜 앱이지만 유화·아크릴 연습 참고용으로 둔다.
   **세부 마무리**: 층이 끝나면 뭉개지 않은 목표 색(`want0`)을 보는 아주 작은 붓 층을 1~2번 더 돈다 (`Rf` = 짧은 변의 0.5~1.5%,
   담채는 `chalk`·유화는 `bristle` 팁, 오차 문턱을 낮춰 원본과 다른 곳만). 창틀·나뭇가지·붉은 벽처럼 작은 것이 이때 살아난다.
   세밀함 45 미만이면 건너뛰고, 75 이상이면 두 번 (임파스토는 소용돌이가 부서지지 않게 70 이상에서 한 번, 붓도 1.6배 크게).
   그림붓(담채·유화·임파스토)은 층마다 목표 색을 붓 크기만큼 뭉갠 것(`blurRGB(want0, R·0.35~0.5)`)을 본다 — Hertzmann 그대로.
   큰 붓이 잎 하나하나에 걸려 짧게 끊기지 않고, 색도 덩어리 평균이 된다. 방향도 잔결(texture > 0.3)에서는 넓은 범위 방향(`field.ctx/cty`,
   반경 28)을 따라 나무 덩어리·물결의 큰 흐름을 탄다 (고흐의 소용돌이). 이걸 안 하면 큰 사진에서 임파스토가 비늘 같은 작은 조각 모자이크가 된다.
   **임파스토**(`brush: 'impasto'`) 는 유화의 변형: 길고 굽은 자국(획 길이 ×5), 자국마다 채널을 따로 흔들어 색상이 조금씩 다르고,
   경로를 모아 두었다가 테두리(0.72×, 넓게) → 몸통 → 능선(1.14×, 좁게) 세 번 얹어 두꺼운 물감이 빛을 받는 느낌을 낸다. 테두리·능선 때문에
   목표와 늘 조금 달라 칸 오차 문턱을 2.4배로 올려 끝없이 덧칠하지 않게 한다. 화풍 `vangogh`(고흐풍, 컬러)가 이 붓을 쓴다 —
   DAP 의 Van Gogh 프리셋 결과(사용자가 올린 그림)를 보고 넣었다.
7. `applyVignette` 로 가장자리 미완성 처리(경계 흔들림 진폭은 여백의 35% — 더 크면 어두운 배경 위에서 톱니처럼 보인다) 뒤 `Canvas.toImage` 로 종이 결을 섞어 내보낸다.

**층별 붓 3벌**(DAP Main Painter 의 붓 그룹 셋): `tip: 'auto'`(그림 화풍 기본)면 `tipForStage` 가 층에 따라 붓을 바꾼다 —
1~2층은 큰 평붓(수채는 젖은 큰 붓), 3~4층은 중간 둥근 붓, 5~6층은 가는 붓(수채는 마른 붓). DAP 화면의 붓 그룹 1·2·3 과 같은 배치다.
층 → 단계는 `k/(passes−1)` 를 0.34·0.67 로 끊어 정한다. 팁을 직접 고르면 모든 층에 그 팁을 쓴다(마지막 층의 젖은 붓만 마른 붓으로).
세부 마무리 층은 언제나 단계 2 의 붓이다.

**획·붓칠의 수**(2026-09 대폭 증가): 사용자가 "붓칠 수와 선 긋는 횟수를 대폭 늘리고, 변환 시간이 길어도 상관없다"고 해 밀도를 올렸다 —
펜 격자 `max(lw·1.5, R·0.21)`, 펜 문턱 `T = 0.015 + 0.15(1−acc)`, 리천 붓 훑기 3벌, 윤곽 2벌, 점묘 2벌에 점 수 상한 420,
펜화 기본 간격 `minSide·(0.016 − 0.0115·detail/100)`(2026-09 더 촘촘하게 조정, 아래 문턱도 `lw·1.6`→`lw·1.35`). 획이 많아져 어두워지지 않도록 `penStroke` 의 정지 조건을 조였다
(`dark > ref + 0.02` 에서 2걸음 뒤 멈춤) — 밀도만 오르고 톤은 그대로다.

**붓 자국이 보이게**(그림붓): 밀도를 올리면 그림이 사진처럼 매끈해진다. 그래서 —
(1) 첫 층 붓을 키우고(`Rmax = minSide·(0.035 + 0.245·brushSize/100)`) 붓칠마다 크기를 흔들며(`rJit` 0.65~1.5),
(2) **뒤 층일수록 문턱을 높여**(`Tk × (1 + 2.6·k/(passes−1))`) 작은 붓이 큰 붓 자국을 덮지 않게 하고 (Hertzmann 의 뜻대로 넓은 면에는 큰 붓 자국이 남는다) —
1~6호 붓 굵기 차이를 벌린 뒤 마지막 붓이 아주 가늘어져 큰 자국을 다 덮고 사진처럼 매끈해져서 1.1 → 2.6 으로 올렸다,
(3) 붓칠마다 색을 흔들고(유화 ±21%, 담채 ±10%), (4) 자국 사이를 벌린다(`step = R·0.55~0.6` — 너무 겹치면 한 줄기 띠가 되어 자국이 안 보인다),
(5) 세부 마무리 층의 문턱도 높여(`max(13, 24(1−acc))`) 세부만 짚게 한다. 붓 방향도 붓칠마다 무작위성만큼 흔들고
(`dirAt(..., (rng()−0.5)·2.2·rnd)`), 방향장에 끌려가는 정도를 무작위성만큼 줄인다 — 안 그러면 다시 일정해진다.

**7층 · 원본 겹치기**(그림붓만): 층과 세부 마무리가 끝나면 표현↔사실이 사실 쪽일 때 원본 사진을 겹쳐 비친다 —
`photoMix = clamp((acc − 0.5)/0.5, 0, 1)³` — 사실 50 부터 시작해 **100 에서는 원본 사진과 같아진다**(사용자 요청).
세제곱이라 가운데(75)는 0.13, 90 은 0.51 로 옅어 붓 자국이 남고, 끝에서만 사진이 된다.
DAP 의 마지막 레이어에 해당하고, 진행 표시에 "7층 · 원본 겹치기"로 나온다.

**원근 선 굵기**(`depth`, 펜 붓만): 가까운 곳은 슬라이더 굵기 그대로(≈0.4~0.6mm), 먼 곳은 최대 0.22배까지 가늘게(≈0.05~0.2mm) 긋는다 —
`wMul(i) = 1 − 0.78·(depth/100)·(1 − near)`. 사진에 깊이 정보가 없으므로 `nearMap` 이 두 단서를 섞어 추정한다: 화면 아래일수록 가깝고(0.4),
지역 대비(원본 − 뭉갠 것의 절대값을 다시 뭉갠 값, 90퍼센타일로 정규화)가 클수록 가깝다(0.6) — 먼 곳은 공기 원근으로 흐려지기 때문이다.
`penStroke`·`loopStroke`·점묘·`edgePass`·`toneHatch` 의 모든 자국에 자리마다 적용된다. 선 굵기 슬라이더의 mm 표시는 긴 변 1000px 을
A4 가로(297mm)로 볼 때의 값이다(1px ≈ 0.3mm).

**세밀할수록 가는 펜**(`detailThin`, 사용자 요청 "인물화·세밀화일수록 펜을 가늘게"): 엔진의 `lw` 에 `1 − 0.5·(detail/100)²` 를 곱한다 —
보통 화풍(세밀함 45~65)은 0.90~0.79 배로 거의 그대로고 인물화·세밀화(100)에서 절반이 된다. 작은 톤 차를 촘촘한 선으로 쌓는 화풍은
선이 굵으면 그 차이가 뭉개지기 때문이다. 점묘(같은 값이 점 굵기)와 그림붓에는 적용하지 않고, 패널의 mm 표시도 이 값을 반영한다.

**화풍별 선 굵기**(사용자 지정): 판화는 많이 굵게(step 17 · 4.52), 고대비는 가는 선과 굵은 선(기준 1.6 에 단계별 폭 0.55~2.15배),
리천은 가는 선과 중간 선(1.76 + 원근 선 굵기 70 이 먼 곳을 0.22배까지 가늘게), 단순은 중간 선 계열(2.34).

**펜촉 결**(`makeNib`, 사용자가 올린 펜 팁 영상 100 종을 보고 넣었다): 영상의 자국들은 **굵기에 따라 모양이 다르다** —
가장 가는 것은 털 한 올의 깨끗한 선이고, 굵어질수록 여러 가닥으로 갈라져 결과 틈이 보이며 가장자리가 부스러진다.
획을 가로지르는 16 점 프로필을 획마다 새로 뽑아(가닥 수 = `min(7, max(2, round(r·1.5)))`, 가닥 사이 골의 깊이 = `0.82 − r·0.13`)
`Canvas.dot` 이 그 자리 덮임에 곱한다. 반지름 0.9 화소 이하(가장 가는 단계)는 결 없이 매끈한 한 줄이다.
`penStroke`(획마다)·`edgePass`(경계선마다)·`toneHatch`(선마다) 세 곳이 쓴다 — 굵은 판화 선이 통짜 막대가 아니라 결 있는 자국이 된다.

**펜 굵기 20 단계**(`PEN_WIDTHS`, 사용자 요청): 펜 붓의 선 굵기 슬라이더는 값이 아니라 **단계**를 고른다 — 1 단계가 화면이 그릴 수 있는
가장 가는 선(긴 변 1000px 기준 1 화소 ≈ 0.3mm)이고 20 단계까지 등비로 굵어진다(1 → 6px, 약 1.8mm). 실제 펜도 0.03·0.05·0.1·0.2… 처럼
가는 쪽이 촘촘하므로 등비로 나눴다. 옛 레코드의 값은 `penWidthStep` 이 가장 가까운 단계로 읽는다. 붓 굵기(그림붓)·점 굵기(점묘)는 예전처럼
값을 그대로 쓴다. 엔진의 `lw` 에는 1 화소 바닥을 두어(`max(1, …)`) 작은 사진에서 선이 사라지지 않는다.

**표현 ↔ 사실**(`accuracy`, DAP 의 Expressive–Realistic): 사실 쪽으로 갈수록 원본에 가까워진다 — 칸 오차 문턱(`T`·`Tk`)이 낮아 획이 더 놓이고,
`washTarget` 의 채도·대비 과장과 팔레트 이동이 줄고(`pk = 1 − 0.65·real`), 세부 마무리 층이 늘어난다(`realD = max(detail, accuracy)`).
**마른 붓 ↔ 젖은 붓**(`wet`, DAP 의 Dry–Wet): 젖을수록 `tipForStage` 가 번지는 붓을 고르고, 획 불투명도가 낮아 옅게 여러 겹 쌓이며
(`×(1.18 − 0.36·wet)`), 층 사이 번짐(`smoothCanvas`)과 안료 고임(`pigmentEdges`)이 커진다. 마르면 자국이 또렷하고 `chalk` 로 갈라진다.
둘 다 `PaintPanel` 의 브러시 팁 바로 아래에 있다.

**브러시 자국 모양**(DAP 의 브러시 모양 목록): 실제 붓 자국은 원이 아니라 제각각 찌그러진 덩어리다. `makeStamp` 이 28×28 격자에
가운데 덩어리 + 곁가지 2~4개 − 구멍 0~2개를 뭉개 만든 모양 여덟 벌(`STAMPS`, 고정 seed)을 미리 두고, `makeTip` 이 **획마다 하나를
무작위로** 고른다. `dabTip` 은 그 모양을 획 방향으로 돌려(평붓은 획 방향으로 0.72배 납작하게) 찍고, 팁 종류에 따라 붓털 줄무늬(bristle)·
화소 노이즈(chalk)·가장자리 안료(wet)를 덧입힌다. 둥근 붓도 이 모양을 쓰므로 완전한 원은 나오지 않는다.

**색 팔레트**(`PaletteId`, DAP 의 Palette): `photo`(사진 색), `bright`(채도 1.55배), `mono`(잉크색 하나의 명암),
`match`(팔레트 12색 중 가장 가까운 물감색으로), `match2`(그 절반만 — Color Match+ 에 해당),
`vangogh`(고흐 12색 — 사용자가 올린 DAP 고흐 프리셋의 색 띠에서 뽑은 크림·푸른 보라·남색·청회색·갈색. `PALETTE_VANGOGH`).
팔레트별 물감색 목록은 `PALETTE_COLORS` 에 있고 `applyPalette` 가 거기서 고른다. `applyPalette` 가 0..1 RGB 를 옮기며,
**색도(밝기를 뺀 색)가 가장 가까운 물감색을 골라 원래 밝기로 되맞춘다** — 비슷한 색끼리 한 물감색으로 묶여 팔레트에 있는 색만 남되
톤은 사진처럼 읽힌다. 묶는 세기는 `strength = clamp((chroma − 0.012)·14) × (match 1, match2 **0.45**)` — 색이 조금이라도 있으면 거의 다
물감색으로 묶고, 회색에 가까운 화소만 원래 색을 남긴다(안 그러면 하늘·물이 원색으로 튄다).
match2 는 0.72 였는데 그러면 12색과 결과가 거의 같아 보여(사용자 지적) 0.45 로 낮췄다 — 12색은 물감색으로 완전히 묶고 혼합은 사진색과 반씩이다.
**붓칠 색도 팔레트를 지킨다**: 층마다 목표 색을 뭉개는 `blurRGB` 가 물감색 사이의 중간색을 만들고 붓칠마다 흔드는 `jit`·`hj` 가 거기서 더 벗어나므로,
`washSweep` 이 **실제로 얹는 색**에 `applyPalette` 를 한 번 더 건다(사용자 요청). 실측으로 팔레트까지의 색도 거리 중앙값이 0.044 → 0.025 로 줄었다.
컬러 모드에서만 쓰이고, 담채·유화의 목표 색(`washTarget`)과 펜·점묘의 획 색(`colorAt`) 양쪽에 적용된다. 팔레트 12색은 `PALETTE_12`(수채 기본 세트).
패널에서는 접이식 목록으로 고르고 **이름 옆마다 그 팔레트의 색 띠**(`PaletteSwatch`)가 붙는다.

**브러시 팁**(`TipKind`, 포토샵 브러시 팁에 해당): `auto`(위 3벌), `round`(원형), `bristle`(평붓 강모 — 획을 가로지르는 24점 붓털 프로필 × 획 방향 짧은 캡슐,
연속 찍으면 붓털 줄무늬가 이어짐), `wet`(젖은 둥근 붓 — 각도별 반지름 흔들림, 가운데 옅고 가장자리로 안료), `chalk`(드라이 브러시 — 털이
성글고 화소 노이즈로 갈라짐), `swirl`(고흐 붓 — 사용자가 올린 DAP 고흐 프리셋의 붓 모양을 보고 넣었다: 길이 1.15R 의 쉼표 자국으로
뒤쪽 2/3 은 붓 폭 그대로 두껍고 앞머리만 가늘어지며 `tip.curl`(획마다 좌/우) 만큼 옆으로 휜다. 붓털 줄무늬는 bristle 과 같다). `makeTip` 이 획마다 새 붓털 배치를 만들고 `Canvas.dabTip` 이 획 방향으로 돌려 찍는다. 담채·유화·임파스토만 쓰고
펜 붓은 둥근 펜촉(`dot`)이다. `PaintProfile.tip` 으로 고르며 `PaintPanel` 의 "브러시 팁" 선택기가 `tipPreview` 로 그린 획 미리보기를 보여 준다.
기본: 수채·유화 `auto`, 고흐 `swirl`. 팔레트 기본: 수채·유화 `match2`, 고흐 `vangogh`, 나머지 `photo`.

`Canvas` 는 색 버퍼 + 어둡기 누적 버퍼다. 획 하나는 `dot()`/`dab()` 들을 모아 `end(alpha, col)` 에서 한 번에 얹는다 — 한 획이 같은 화소를
두 번 칠하지 않도록 화소별 최대 덮임만 남기고, 획끼리는 screen 방식으로 진해진다. 컬러 모드의 획 색은 사진 색을 잉크색과 섞어 누른 것.

**진행 표시**: `renderDrawing` 의 `onProgress` 가 층이 끝날 때와 도중 0.2초마다 중간 그림을 내보낸다. 워커는 이를 `progress` 메시지로
보내고(`render.worker.ts`), `local.ts` 가 콜백으로 넘기고, `App` 이 `progress` 상태로 들고 `Stage` 의 `LiveCanvas` 가 캔버스에 바로 찍는다
(Blob 인코딩 없음). 툴바 "중단" 버튼에도 퍼센트가 뜬다. 화면 스레드 대체 경로에서는 중간 그림이 없다.

방향장 `orientationField` 는 세 채널 구조 텐서(반경 7, 없으면 28)로 면의 방향을 구하고, 잔결(`textureMap` = 색 경계 화소 밀도)에서는
확실성을 눌러 기준 각도로 돌아가게 한다 — 단 넓은 범위 확실성 `aniso`(반경 28) 가 높으면(물결·풀처럼 한 방향으로 흐르는 잔결) 방향을 살린다.
잔결(texture > 0.5) 안에서 `dirAt` 은 억제 전 지역 확실성 `raw` 가 높으면 지역 방향을, 아니면 기준 각도 주변의 무작위 각을 준다 —
획이 짧기(선 굵기 ×14 이하) 때문에 잎 무리의 질감으로 읽힌다. 실사 시험(호수·숲·정자 사진, DAP 작업 영상에서 잘라낸 것)에서 이 조합이
기준 각도 긴 줄(풀밭처럼 보임)이나 방향장 추종 긴 획(벌레처럼 보임) 둘 다를 이겼다. 나뭇잎 고리선은 잔결이면서 `aniso` 가 낮은
곳(방향 없는 잔결)에서만, 첫 훑기에서만, 옅게 그린다 — 둘째 훑기(그림자 교차)는 해칭으로 잎 그늘을 만든다.
목표 어둡기는 `0.06 + 0.84·d^1.1`, 잔결 감산은 0.22 — 더 옅게 하면 실사에서 나무 덩어리의 톤 차가 사라져 아무것도 읽히지 않는다.
**큰 어두운 배경**(스튜디오 인물 사진의 검은 배경, 밤 하늘): 펜 화가는 비워 두고 인물의 윤곽만 남긴다. `bgMask` 를 만들어 목표 어둡기와 잔결을 그만큼 지운다.
**그늘진 나무를 배경으로 오인하면 안 된다** — 사용자 제보: 호수 사진의 어두운 물가 나무 덩어리가 통째로 흰 종이가 됐다.
가르는 기준은 경계의 절대 세기가 아니라 **그 자리 밝기에 대한 비율** `rel = 뭉갠 색 경계 / (뭉갠 밝기 + 0.04)` 이다 —
깊은 그늘은 사진이 곱셈으로 어두워진 것이라 절대 경계는 작아도 비율은 밝은 곳과 같고, 스튜디오 배경·밤 하늘은 비율로 재도 센서 노이즈뿐이다.
실측값이 스튜디오 배경 0.28 · 그늘진 숲 0.72 · 밝은 사진의 어두운 화소 2.3 이라 문턱을 0.45 로 잡았다(그늘 사진을 흉내 낸 시험에서
빈 종이가 되던 39% → 0.5%, 스튜디오 배경은 그대로 비워진다). 조건은 넷이다: 뭉갠 밝기 < 0.13, 뭉갠 색 경계 < 0.14, `rel` < 0.45,
잔결 < 0.15. 여기에 **그런 화소가 화면 전체의 14% 이상**(스튜디오 배경은 40~60%, 풍경의 그늘은 그보다 훨씬 작다)이고
그 자리 둘레(짧은 변/8)의 55% 이상이 그런 화소여야 한다. `Ctx.bg` 로
`edgePass` 에도 넘겨 배경에서는 윤곽 씨앗을 잡지 않고 들어온 획도 멈춘다 — 이걸 안 하면 검은 배경이 화면 절반을 덮는 교차 해칭 덩어리가 되고
센서 노이즈마다 짧은 윤곽이 그어진다. 밝기·경계를 뭉개서 판단하는 것이 핵심(원본 화소로 재면 노이즈에 속는다). 그림붓에는 적용하지 않는다.

**밝기 구역 자동 맞춤**(`autoLevels`, 모든 붓): 여백 문턱은 밝기 0~1 의 **절대값**이라, 가장 밝은 곳이 0.2 뿐인 밤·역광·검은 배경
사진은 화면 전체가 문턱 아래로 들어가 통째로 새까만 덩어리가 됐다(사용자 제보 — 스튜디오 인물 사진에서 얼굴까지 사라졌다).
`renderDrawing` 첫머리에서 사람이 하는 일을 그대로 한다: 사진의 가장 밝은 곳과 가장 어두운 곳(2·98 퍼센타일)을 찾고,
그 사이를 `TONE_ZONES`(15) 개의 명암 구역으로 나눠 종이의 값 범위(0.03~0.97)에 고르게 펼친다. 흰 여백이 반드시 생기지는 않는다 —
여백은 여백 슬라이더가 정하고 여기서는 값의 폭만 되찾는다.
섞는 것은 둘이다: (1) 2~98 퍼센타일을 펴는 단순 확대(사진의 명암 관계가 그대로 남는다), (2) 15 구역이 값 범위를 똑같이 나눠 갖는
재배분(어두운 쪽에 몰린 사진에서 그림자 속 형태가 갈라진다). 구역 안에서는 원래 밝기 순서대로 이어 붙이므로 계단(포스터라이즈)이
생기지 않는다 — 톤을 단계로 눌러 버리는 아래 "실패한 시도"와 다른 점이 이것이다.
**밝은 쪽이 제자리(98 퍼센타일 ≥ 0.72)인 사진은 아예 손대지 않고**, 그보다 어두울수록 재배분을 세게 섞는다(`0.55·strength`) —
흰 배경 인물 사진까지 재배분했더니 배경이 어두운 구역으로 밀려 새까매졌다(시험함).

펜 붓의 목표는 사람 화가처럼 단순화한다: 잔결 영역은 `rS·2`(rS 는 세밀함에서, 짧은 변의 0.8~3.8%) 로 크게 뭉개고 그 밖은 `rS·0.4` 로 살짝
뭉갠 뒤, 덩어리 크기(짧은 변/70)의 지역 대비를 키운다(`+ (target − blur) · 0.9 · texture`) — 잎 무리의 밝은 덩어리와 그늘 덩어리가 갈라져야
나무로 읽힌다. 잔결이 없는 곳(얼굴·벽)의 블러 반경은 `rS·0.15` 로 아주 작다 — 눈·입·주름 같은 작은 톤 차가 곧 인물화다.
먹 채움은 잔결 0.75 까지 허용해 잎 그늘의 검은 점을 낸다. **실패한 시도(다시 하지 말 것)**: 톤을 단계로 누르기(포스터라이즈),
잔결에서 어두운 곳만 남기기, 뭉갠 톤 지도의 경계를 윤곽으로 긋기, 밝기 분포에 따른 적응형 여백 문턱 — 모두 실사 숲 사진에서 섬 윤곽이
위장 무늬처럼 되거나 풍경의 언덕·물이 이상하게 비워졌다. 잔결 안의 획은 짧게 두되 화면 크기에 비례(짧은 변의 3.5% 이상)시킨다 —
선 굵기에만 묶으면 큰 사진에서 부스러기 털처럼 보인다.
빽빽한 숲 사진의 펜 결과는 여전히 이 엔진의 약점이다(톤은 읽히지만 잎 질감이 거칠다). 구조물이 있는 사진에서 훨씬 낫다. 고리선은 `randomness` ≥ 25 인 설정에서만 — 정돈된
손(세밀 펜화, 8)은 고리 대신 짧은 잎 획이다. 지시선(`manualField`)이 가까우면 그 방향으로 끌어당기고 `man` 가중치를 함께 내보내
형태 따라가기 값과 무관하게 먹게 한다.

순수 DAP 방식으로 "필요한 곳에만 짧은 획"을 놓으면 펜 선이 노이즈가 된 적이 있다(옛 엔진 시절 시험). 지금 엔진이 해칭으로 읽히는 이유는
(1) 격자 간격이 선 굵기에 묶여 이웃 획이 일정 간격으로 놓이고, (2) 한 층 안에서 방향이 `dirAt` 으로 매끈하게 이어지고,
(3) 획이 길고(획 크기 × 획 길이) 다른 면에서만 멈추기 때문이다. 이 셋을 흔들면 다시 노이즈가 된다.

두 작가 프리셋의 기준: `RICHEON_PAINT` 는 사용자(앱 주인)가 올린 자기 드로잉 4장 — 건물 하나를 가운데 두고 하늘·땅은 흰 종이, 중간 굵기 펜,
면을 따르는 해칭, 잎은 고리 뭉치, 창·문 안은 먹, 가장자리 미완성 — 에 맞춰 여백 66·굵기 1.4·잉크 88·윤곽 80 이다. `FINE_PAINT` 는 사용자가
올린 세밀 펜화 8장(작가 이름은 문서·UI 에 쓰지 않음) — 아주 가는 펜, 평면마다 한 방향의 길고 고른 해칭, 하늘은 수평 해칭 속 흰 구름, 그림자는
먹, 잎은 짧은 잎 획 — 에 맞춰 무작위성 8·획 길이 80·형태 따라가기 90·잉크 92 다.

화풍(`PenStyle`) 15종마다 `PAINT_FOR_STYLE` 에 완전한 설정이 있다 (DAP 의 프리셋) — 고대비·리천·인물화·부분칼러·단순·
윤곽선·점묘·판화·세밀화·일러·카버·수묵담채·수채 담채·유화·고흐풍. 스크리블·어반 스케치·건축 제도·지브리·웹툰·망가는
지웠고, 옛 레코드는 `mergeParams` 의 `OLD_STYLE` 이 가장 가까운 화풍으로 옮긴다. 갤러리나 드롭다운에서 화풍을 고르면 `App` 의 효과가
`PAINT_FOR_STYLE[style]` 의 완전한 설정을 `params.paint` 에 넣고,
견본이 있으면 `blendPaint(base, measured, referenceWeight)` 로 섞는다. 이력에서 불러올 때만 `keepStrokesRef` 로 그 재설정을 한 번 건너뛴다.
새 화풍을 추가할 때는 `PenStyle`, `PAINT_FOR_STYLE`, `STYLE_LABEL`·`STYLE_DESC`, `STYLE_TEXT`, `STYLE_TIP`, `public/presets/<style>.jpg` 를 함께 넣는다.
옛 ID `parkyongsoon` 은 `mergeParams` 가 `fineink` 로 바꾸고, 옛 레코드의 `strokes`(StrokeProfile)는 `migrateStrokes` 가 `paint` 로 옮긴다.

### 견본 분석

`sampleStyle.ts` 의 `analyzeSample` 이 견본 드로잉을 재어 `PaintProfile` 로 돌려준다. 측정은 근사치이므로 **슬라이더가 항상 최종 권한**이다.
측정 방법: 큰 블러로 나눠 조명 평탄화 → Otsu 로 잉크 마스크 → 16px 블록 밀도로 여백·톤 단계(→ 층 수)·해칭 영역 →
침식 횟수로 선 굵기 → 그래디언트 방향 히스토그램으로 각도(→ 기준 각도)·집중도(→ 무작위성)·2차 봉우리(→ 교차 붓) →
해칭에 수직인 선을 따라 잉크 시작 횟수로 간격(→ 세밀함), 잉크 위에서 출발해 선을 따라간 길이로 획 길이와 점묘 여부.
형태 따라가기·정밀도는 견본 한 장으로 재기 어려워 기본값을 둔다.
로컬 결과가 떠 있는 상태에서 그리기 설정이나 색을 바꾸면 350ms 뒤 자동으로 다시 그려 같은 이력 항목을 갱신한다.

### 해칭 방향 지시선 (DAP 의 수동 Feature Follow)

`DrawingParams.guides`(`DirectionGuide[]`, 그림 상대 좌표 0~1)와 `guideRadius`(짧은 변의 %). 툴바 "방향 지시"를 켜면
`Stage` 의 `DirectionLayer` 가 원본 위에서 포인터로 선을 받는다. 해칭선은 양쪽으로 뻗으므로 화살표는 없다.
엔진(`render.ts`)의 `manualField` 가 지시선을 4px 격자에서 거리 가중(가우시안, σ = radius·짧은 변·0.5)한 방향장으로 바꾸고,
`orientationField` 가 두 배 각 벡터로 자동 방향장과 섞는다 — 가까울수록 지시선을, 멀어질수록 자동 방향을 따르고
`coh` 와 `man` 을 함께 끌어올려 평탄한 곳(하늘·벽)에서도, 형태 따라가기가 0 이어도 지시가 먹는다. 모든 붓의 획이 방향장을 따르므로
붓에 따른 예외는 없다.
지시선은 사진이 바뀌면 비워지고, 실시간 재렌더의 비교 대상에 포함되며, `Drawing.params` 에 저장돼 이력에서도 재현된다.

### 화풍 프리셋 갤러리 (DAP 의 프리셋 탭)

`presetGallery.ts` + `public/presets/<style>.jpg`. **예시 그림을 바꾸면 `presetGallery.ts` 의 `PRESET_VERSION` 도 올린다** —
Vite 는 `public/` 파일에 해시를 붙이지 않아 파일 이름이 같으면 브라우저가 옛 그림을 캐시에서 계속 보여 준다(주소 뒤 `?v=` 로 막는다). `StylePanel` 맨 위 `.gallery` 격자가 화풍마다 예시 그림을 보여 주고,
누르면 `onParams({ style })` 만 한다 — 화풍이 바뀌면 `App` 의 효과가 `PAINT_FOR_STYLE` 의 완전한 그리기 설정을 넣는다 (위 로컬 엔진 절).
썸네일에 마우스를 올리면 그 예시 그림을 300px 로 크게 띄우고 화풍 이름·설명(`STYLE_DESC`)을 함께 보여 준다(`.preset-peek`) —
갤러리 아래에 늘 떠 있던 설명 줄은 지웠다. `.panel` 에 `backdrop-filter` 가 걸려 있어
패널 안에서는 `position: fixed` 가 패널 기준이 되므로 **`createPortal` 로 body 에 내보내** 화면 기준으로 자리를 잡는다.
예시 그림 15장 중 일부는 같은 사진(컵과 배)을 이 앱의 로컬 렌더러로 그린 것(480px JPEG)이고, 나머지는 **사용자가 올린 실제 그림**
(리천·윤곽선·고대비·단순·점묘법·판화·세밀화·수채 담채·유화)으로 바꿔 두었다 — 화풍의 기준이 되는 그림이라 AI 견본으로 보낼 때도 이쪽이 낫다.
`scratchpad/mkpreset.mjs` 가 올린 사진의 여백을 잘라 480×360 으로 맞춘다(4:3 에 가까우면 꽉 채우고, 세로 그림은 종이색 여백을 둔다). 세로 그림은 4:3 상자 안에 종이색 여백을 두고 넣는다(갤러리 썸네일이 `object-fit: cover` 라 잘리지 않게). 새 화풍을 추가하면 같은 이름의 예시 그림도 넣는다. AI 로 그릴 때 견본도 로컬 결과도 없으면
`fetchPresetImage` 로 그 예시 그림을 두 번째 이미지로 보내고
`RefKind` `preset` 으로 "다른 사진이니 기법만 따르라"고 지시한다. 못 가져오면(오프라인·미리보기) 견본 없이 그린다.

### 조정 패널의 단순화

**펜화와 붓화를 나눠 보여 준다**: `StylePanel` 의 갤러리는 "펜 화풍"과 "붓 화풍" 두 묶음이고(`PAINT_FOR_STYLE[st].brush` 가
wash·oil·impasto 면 붓 화풍), `PaintPanel` 에는 "펜으로 그리기 / 붓으로 그리기" 탭만 있다 — **붓 하나하나를 고르는 줄(펜화·리천·윤곽·점묘 / 수묵·담채·유화·고흐)은 지웠다**,
붓은 드로잉 프리셋이 정하기 때문이다(사용자 요청). 탭은 그쪽에서 마지막에 쓰던 붓으로 바꾼다.

**화풍 프리셋은 프리셋 / 즐겨찾기 두 탭**이다(`StylePanel` 의 `tab` 상태): 즐겨찾기 탭은 저장해 둔 그리기 설정
(`PresetList` — 옛 `PaintPanel` 의 "즐겨찾기" 칸을 여기로 옮겼다), 프리셋 탭은 예시 그림 갤러리(펜 화풍·붓 화풍)다.
**화가**(유명 화가 10명 드롭다운, AI 전용)는 탭 밖 색 표현 바로 위에 있고, 강도는 접힌 "화풍 목록 · 밝기 · 대비"로 들어갔다 —
화풍·화가·색 표현·빛 방향은 로컬·AI 가 함께 보는 설정이라 탭에 넣지 않는다.

오른쪽 패널은 초보자가 무엇을 만져야 할지 알 수 있게 **자주 쓰는 것만 밖에** 둔다. `StylePanel` 기본 화면에는 화풍 갤러리(로컬/AI 탭)·색 표현·
빛 방향, `PaintPanel` 기본 화면에는 붓 여덟(+그림붓이면 브러시 팁)과 슬라이더 넷 — 세밀함(펜화면 선 간격)·여백·진하기·선 굵기(점묘는 점 굵기) — 뿐이고(펜화 붓은 명암 단계가, 리천·윤곽 붓은 형태 따라가기가 하나씩 더 붙는다. 선 방향 슬라이더는 없앴다), 슬라이더 값 옆에는
숫자 대신 사람 말("큰 형태만", "흰 종이를 넓게", "가는 펜 1.2px")을 쓴다. 나머지는 `<details className="advanced">` 두 곳에 접어 둔다:
`PaintPanel` 의 "세부 조정 · 획 · 방향 · 색"(기본 프리셋 3개, 층 수, 획 크기, 획 길이, 무작위성, 원근 선 굵기, 윤곽선,
가장자리 여백, 종이·잉크색)과 `StylePanel` 의 "화풍 목록 · 밝기 · 대비"(화풍 드롭다운, 밝기, 대비).
기본 화면에 보이는 슬라이더는 넷뿐이다 — 늘리기 전에 이 원칙을 먼저 확인한다. 화풍 드롭다운이 접혀 있으므로 Playwright 로 화풍을 바꿀 때는
갤러리 썸네일을 누르거나 `details.advanced` 를 먼저 열어야 한다.

### 즐겨찾기 프리셋

`presets.ts`. 현재 `PaintProfile` 을 이름 붙여 localStorage `oilpen.presets.v2` 에 둔다(최대 24개). v1(옛 선·톤 프로필)은 불러올 때
`migrateStrokes` 로 옮긴다. 저장·적용·삭제 UI 는 `components/PresetList.tsx` 이고 **화풍 프리셋의 "즐겨찾기" 탭**에 있다.
현재 값과 같은 프리셋을 `samePaint` 로 표시한다 (내장 3개는 `PaintPanel` 의 접힌 "기본 프리셋" 줄에 그대로 있다).
적용은 `patchPaint` 로 `paint` 만 바꾸므로 화풍(`PenStyle`)은 그대로다.

**갤러리 프리셋 덮어쓰기**(`StylePaints`, localStorage `oilpen.stylePaint.v1`): 갤러리 썸네일 모서리의 ＋(또는 썸네일 오른쪽 단추)를
누르면 지금 그리기 설정을 그 화풍의 설정으로 저장한다 — 프리셋을 자기 손에 맞게 길들이는 것이다. 저장해 두면 `App.paintFor` 가
`PAINT_FOR_STYLE[style]` 대신 이 값을 쓰고, 그 칸의 단추가 ↺(원래 프리셋으로) 로 바뀌며 늘 보인다. 즐겨찾기와 달리 이름이 없고
화풍 하나에 하나뿐이다.

### 외부 결과 불러오기 (삭제됨)

입력 패널의 "외부 결과" 칸(다른 프로그램의 결과를 이 사진의 완성 참고로 불러오던 자리)과 `App.importExternal` 은 없앴다.
`Engine` 의 `'external'` 값만 옛 이력 레코드를 읽기 위해 남아 있다 — 새로 만들지 않는다.
`docs/dap-workflow.md` 의 불러오기 절차도 지금은 쓰이지 않는다.

### 낙관·사인

`stamps.ts`. 등록 항목(`StampItem`, 투명 PNG data URL)과 배치(`PlacedStamp`, 그림에 대한 상대 좌표 0~1, 폭 비율, 투명도)를
localStorage `oilpen.stamps.v1` 에 둔다. 낙관 2개·사인 3개 한도. 항목마다 **적용/비적용 토글**(배치를 넣고 뺀다)과
크기·**투명도** 슬라이더가 있고, 투명도는 `compositeStamps` 의 `globalAlpha` 와 끌기 중 오버레이 양쪽에 쓰인다
(옛 레코드에는 없어 `STAMP_OPACITY` 0.95 를 기본으로 본다). `Drawing.base` 가 찍기 전 결과이고 `result` 는 배치를
구워 넣은 것이라, 전체화면·저장은 `result` 를 그대로 쓴다. `Stage` 는 끌기 중 부드럽게 보이도록 `base` 위에
DOM 오버레이(`StampLayer`)로 그리고, 놓는 순간 `App.rebake` 가 `compositeStamps` 로 다시 굽는다.

### 제공사 (BYOK)

`providers/` 아래 세 곳이 `ImageProvider` 인터페이스(`common.ts`)를 구현한다.
`EDITS_INPUT`(`providers/index.ts`)이 중요한 갈림길이다.

- **gemini, openai**: 사진을 직접 편집한다. 원본 구도가 유지된다.
- **xai**: 이미지 편집 API 가 없어 **비전 모델로 사진을 묘사 → 그 글로 새로 생성**하는
  2단계다. 구도 재현이 덜 정확하고, UI 가 이 사실을 사용자에게 알려 준다.

모델 ID 와 기본 URL 은 사용자가 설정 화면에서 바꿀 수 있다 — 제공사가 모델 이름을 바꾸거나
CORS 로 막을 때 코드 수정 없이 대응하기 위한 것이므로, 모델 ID 를 코드에 못박지 않는다.

`callApi`(`common.ts`)가 네트워크/HTTP 오류를 한국어 메시지 + 힌트로 바꾼다.
새 제공사를 붙일 때도 이걸 거쳐야 오류 표시가 일관된다.

### 빛의 방향

`DrawingParams.lightAuto`. **기본은 "원본"** — 사진의 빛을 그대로 쓴다. 사진을 올리면 `estimateLight`(`image.ts`)가 뭉갠 밝기의
평균 기울기로 8방향 중 하나를 골라 다이얼에 넣어 두지만, `lightAuto` 가 켜져 있는 한 사진은 손대지 않는다.
사용자가 다이얼을 돌리면 `lightAuto=false` 가 되고, `prepareInput` 의 `relight` 가 사진에 그 방향의
밝기 기울기(multiply + screen)를 입힌다. `LightDir` 에는 8방위 말고 방향이 없는 둘이 더 있다 —
`front`(앞에서·정면광: 중간 회색 쪽으로 당겨 대비를 낮추고 가운데를 올려 그림자를 지운다)와
`back`(뒤에서·역광: 가운데를 가라앉히고 가장자리를 밝혀 실루엣과 테두리 빛을 만든다).
다이얼 아래 세 버튼(원본 · 앞에서 · 뒤에서)이 이 셋을 고르고, 8방위 점은 `LIGHT_DIRS`(= `CompassDir`)로 따로 둔다. 편집형 제공사와 로컬 렌더러 모두 글이 아니라 사진의 명암을 따르므로
빛 방향은 이렇게 **사진을 바꿔서** 전달한다. 지시문은 자동이면 "사진의 조명을 유지", 수동이면 "다시 조명된 사진을 따르라"고 쓴다.
실시간 재렌더는 빛이 바뀌면 원본 `File` 에서 다시 준비하므로, 이력에서 불러온(원본 파일이 없는) 결과에는 적용되지 않는다.

### 지시문 조립

`prompt.ts` 의 `buildPrompt` 가 `DrawingParams` 를 문장들로 조립한다. 순서가 의미를 갖는다:

```
매체 선언(mediumText) → 구도 유지 → 원본 비율(aspectText) → 화풍(STYLE_TEXT, "PRIMARY STYLE")
→ 화풍 금지 사항(STYLE_AVOID) → 그리기 설정(paintText) → 화가(artistText) → 강도 → 색
→ 빛 방향 → 톤 → 바탕 질감 → (견본이 있으면) 견본 반영 → 화풍 한 번 더
```

**프리셋마다 결과가 달라야 한다**(사용자 지적: 프리셋이 다른데 비슷하게 그렸다). 지시문이 길면 모델이 모든 항목을
평균 내므로 세 가지로 못을 박았다: (1) 화풍을 맨 앞에 "PRIMARY STYLE — 나머지는 이 안에서의 조절이고 이것을 누그러뜨릴 수 없다"로
두고, (2) `STYLE_AVOID` 로 화풍마다 **하지 말 것**을 적고(점묘에 선 금지, 유화에 윤곽선·흰 바탕 금지 …),
(3) 긴 글은 끝 문장이 가장 세게 남으므로 맨 끝에서 화풍을 한 번 더 말한다.

**매체가 붓을 따른다**: 담채·유화·임파스토는 펜 그림이 아니므로 `mediumText` 가 첫 줄("수채화지 위의 잉크+수채",
"캔버스 위의 유화")과 마지막 질감 줄을 바꾼다. 옛 지시문은 모든 화풍에 "볼펜 드로잉 · 종이 결 · 펜 자국"을 붙여
붓 화풍이 펜 쪽으로 끌려갔다. `paintText` 도 붓을 본다 — 점묘에는 펜 굵기 대신 점 굵기, 그림붓에는 먹 채움·윤곽선 문장을 빼고 붓 크기를 쓴다.

**원본 비율**(사용자 지적: AI 결과가 정사각형으로 나온다)은 세 곳에서 지킨다 — `aspectText` 가 지시문에 "사진과 같은
가로:세로, 자르거나 여백을 넣지 말 것"을 쓰고, `App.drawAi` 가 `imageSize(preparedInput)` 를 `GenerateRequest.aspect` 로 넘기며,
제공사가 `nearestRatio`(`providers/common.ts`)로 자기가 낼 수 있는 크기 중 가장 가까운 것을 고른다:
OpenAI 는 `size`(1024×1024 · 1536×1024 · 1024×1536, 옛 `'auto'` 는 정사각형으로 나오기 일쑤였다), Gemini 는
`generationConfig.imageConfig.aspectRatio`(모르는 옛 모델이면 400 을 받고 비율 없이 재시도). xAI 는 크기 인자가 없어 지시문에만 의존한다.

`paintText` 는 로컬 엔진의 `PaintProfile` 을 문장으로 옮긴 것이라 두 경로가 같은 설정을 본다.
`buildPrompt` 의 두 번째 인자 `RefKind` 가 두 번째 이미지의 정체다: `sample`(올린 견본), `local`(같은 사진의 로컬 결과,
`DrawingParams.aiRefFromLocal` 이 켜져 있고 로컬 결과가 떠 있을 때 `App.drawAi` 가 `current.base` 를 보냄), `preset`(둘 다 없을 때
고른 화풍의 갤러리 예시 그림). 우선순위는 local → sample → preset. 로컬을 보낼 때는
"손으로 다시 그리되 기계적 규칙성은 베끼지 말라", 프리셋은 "다른 사진이니 주제는 무시하고 기법만 따르라"고 덧붙인다.

화풍(`PenStyle`, 15종)은 기법이고, 화가(`ArtistId`, `artists.ts`, 10명)는 그 위에 얹는
해석이다. 둘은 곱해서 쓴다. 화가 목록은 **사후 60년 이상 지난 작가만** 넣는다 —
저작권 문제와 이미지 생성 API 의 이름 거부를 함께 피하기 위해서다. 각 항목이 이름뿐 아니라
선·명암 기법 서술(`prompt` 필드)을 갖는 이유도 모델이 이름을 몰라도 특징이 남게 하기 위함이다.

### 데이터 보관

- 설정·API 키: `storage.ts` → `localStorage`(기억하기 켬) 또는 `sessionStorage`(끔).
  **서버로 보내지 않는다.** 요청은 브라우저에서 제공사로 직접 간다.
- 이력: IndexedDB 에 최근 60개 (`Drawing` 레코드에 입력·견본·결과 Blob 포함. 옛 레코드의 `process` 는 읽기만 한다).
  툴바 띠에는 최근 8장만 보이고, "모두 보기" 단추가 `HistoryModal` 을 열어 전부 보여 준다 — 그림을 누르면 화면으로 불러오고,
  칸마다 ⤓ 로 그 그림만 PNG 로 저장한다(`App.downloadDrawing`, 툴바 저장과 같은 길).

이력에서 불러온 `params` 는 `mergeParams` 로 기본값과 병합한다(`paint` 는 화풍 프리셋 위에 덧씌우고, 옛 `strokes` 는 `migrateStrokes` 로 옮긴다) —
필드를 새로 추가하면 옛 레코드에 그 값이 없기 때문이다. `Drawing.engine` 이 없으면 옛 AI 레코드다.

### 미리보기 모드

`env.ts` 의 `IS_PREVIEW`. 단일 HTML 로 묶어 Artifact 로 올린 빌드에서는 외부 API 호출이
차단되므로, 배너와 안내를 띄우고 키 없이도 화면을 둘러볼 수 있게 한다.

## 배포

`.github/workflows/deploy.yml` 이 main 또는 작업 브랜치 푸시에 Pages 로 배포한다.
저장소 변수 `PAGES_DOMAIN` 이 있으면 CNAME 을 쓰고 `VITE_BASE=/` 로 빌드한다.
Pages 는 공개 저장소에서만 동작하고, Source 를 "GitHub Actions" 로 두어야 한다.

Windows 사용자는 `start.bat` 더블클릭으로 실행, `update.bat` 으로 갱신한다.

## 문서

- `docs/design-proposal.md` — 파라미터 정의, 화풍·화가 목록. **옛 설계라 지금과 다른 곳이 있다**: 숙련도 3단계와
  "흑백으로 변환하여 입력" 토글은 없앴고, 화풍은 15종이다.
- `design/*.dc.html` — 초기 화면 목업 (기본안과 대안 B·C). 현재 UI 는 대안 B 를 구현한 것
